{
  "name": "logscrub",
  "version": "0.3.0",
  "private": true,
  "description": "Browser-based PII redaction tool",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build:wasm && npm run --workspace=web dev",
    "build": "npm run build:wasm && npm run --workspace=web build",
    "build:wasm": "cd packages/wasm-core && wasm-pack build --target web --out-dir pkg",
    "preview": "npm run --workspace=web preview",
    "deploy": "npm run build && node scripts/deploy.mjs"
  },
  "keywords": [
    "pii",
    "redaction",
    "privacy",
    "wasm",
    "log-sanitizer"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "basic-ftp": "^5.1.0",
    "dotenv": "^17.2.3"
  }
}
